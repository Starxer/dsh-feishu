# The plugin stays active while unconfigured so it can serve its local settings page.
- insert:
    - id: lark-channel
      name: '@starxer/ds-feishu'
      disabled: false
      config:
        appId: ''
        appSecretRef: DSH_LARK_APP_SECRET
        domain: feishu
        requireMention: true
        dmMode: open
